[package]
name = "hunt_codebase"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.0"

#this is to make small amount of optimization in the dev profile
[profile.dev]
opt-level=1
#and this to make large amount of optimization in th edev profile for dependencies
[profile.dev.package."*"]
opt-level=3